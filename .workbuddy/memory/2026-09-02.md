# 2026-09-02 工作日志

## 朋友借条网站（欠债项目）——本地联调完成

### 本轮完成
1. **修复 repay.html `rmbUpper is not defined` bug**（第323行调用、第242行残留 toCn，函数从未定义——之前只修了 admin.html，repay.html 漏修）。已把 admin.html 中标准 rmbUpper 实现复制到 repay.html，toCn 清零。
2. **浏览器实测（agent-browser，iPhone 视口）**：
   - 管理端借条详情弹窗：借条全文含人民币大写"捌仟元整"，rmbUpper 生效
   - 借款人还款页（/p/:token）：逾期93天提示、借条确认弹层、确认后还款表单展开、凭证上传提交全链路正常
   - 管理端确认到账 → 借条自动转 paid（847868分=847868分精确结清）
3. **回归测试：flow_test.py 20 pass 0 fail**（全新库运行）
4. demo 数据已恢复干净演示态（张伟5万8%/李娜2万无息/王强8千12%已逾期，全部待借款方确认）
5. 验证截图存于 `screenshots/`（6张）：admin_detail_fixed / admin_paid_detail / repay_contract_modal / repay_overdue_confirm_wait / repay_after_confirm_payform / repay_voucher_submitted

### 关键经验
- **repay.html 和 admin.html 是两套独立前端**，共享函数（如 rmbUpper）必须双端同步修改，单端修复会造成另一端口 ReferenceError
- 浏览器提交按钮点击时若 ref 过期会静默失败，需 eval 直调函数或用 curl 验证 API 层分离问题
- 测试需全新库时：cp db.json 备份 → rm → 重启 → 跑完 → 恢复

### 当前状态
- 本地服务 PORT=8931 运行中（后台任务），demo 库 3 笔借条
- 登录密码：123456（仅测试用，上线前须改）
- 待办：部署腾讯云（子域名候选 qian.hhtc.top）+ PM2 + HTTPS、git 推送到 qianyu1987/pay、上线前重置密码与 demo 数据
